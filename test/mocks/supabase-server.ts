export const serverSupabaseServiceRole = () => {
  throw new Error('serverSupabaseServiceRole not mocked — use vi.mock in test')
}
export const serverSupabaseUser = () => {
  throw new Error('serverSupabaseUser not mocked — use vi.mock in test')
}
